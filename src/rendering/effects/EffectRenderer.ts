import type { GameEvent } from '../../game/simulation/types';import type { RenderCommand,VisualIntentSpec } from '../types';export const CANONICAL_VISUAL_INTENTS:readonly VisualIntentSpec[]=Object.freeze([{id:'incoming-damage-direction',intent:'threat',channel:'edge',question:'Where did the damaging attack come from?',reason:'Direction changes the immediate dodge, cover and retaliation decision.',duration:'transient'},{id:'hit-confirmation',intent:'confirmation',channel:'reticle',question:'Did my aimed attack actually connect?',reason:'Confirmation determines whether to keep tracking, correct aim or change target.',duration:'transient'},{id:'weapon-ready',intent:'readiness',channel:'reticle',question:'Can this deliberate weapon fire again now?',reason:'Readiness changes shot timing for weapons with meaningful recovery windows.',duration:'transient'},{id:'sniper-focus',intent:'state',channel:'reticle',question:'How close is this precision shot to full focus?',reason:'Focus changes whether holding, releasing or aborting the shot is optimal.',duration:'while-active'},{id:'critical-health',intent:'threat',channel:'edge',question:'Am I in a health state where another mistake is likely lethal?',reason:'Critical health changes aggression, cover, disengage and repair decisions.',duration:'while-critical'},{id:'controller-command-space',intent:'spatial',channel:'world',question:'Where is my swarm currently being asked to operate?',reason:'Command position is battlefield information required to steer the swarm.',duration:'while-commanded'},{id:'hostile-drone-commitment',intent:'threat',channel:'world',question:'Which hostile drone has committed an attack line toward me?',reason:'The line creates a short movement or direct-fire counterplay decision.',duration:'while-active'},{id:'chassis-material-identity',intent:'identity',channel:'chassis',question:'What kind of machine and lineage am I looking at?',reason:'Quiet material cues improve recognition without implying temporary state.',duration:'persistent'}]);function point(p:unknown){if(!p||typeof p!=='object')return;const v=p as Record<string,unknown>,x=v.x,y=v.y;return typeof x==='number'&&Number.isFinite(x)&&typeof y==='number'&&Number.isFinite(y)?{x,y}:undefined}function color(p:unknown,f:string){if(!p||typeof p!=='object')return f;const v=(p as Record<string,unknown>).color;return typeof v==='string'&&v.length>0?v:f}export class EffectRenderer{build(events:readonly GameEvent[]):readonly RenderCommand[]{const out:RenderCommand[]=[];for(const e of events){const p=point(e.payload);if(!p)continue;if(e.type==='weapon-fired'||e.type==='muzzle-flash')out.push({kind:'circle',layer:'effect-over',x:p.x,y:p.y,radius:7,stroke:color(e.payload,'#eafcff'),lineWidth:1.4,alpha:.72});else if(e.type==='projectile-impact'||e.type==='entity-hit')out.push({kind:'circle',layer:'effect-over',x:p.x,y:p.y,radius:12,stroke:color(e.payload,'#c8efff'),lineWidth:1.6,alpha:.64});else if(e.type==='entity-destroyed')out.push({kind:'circle',layer:'effect-over',x:p.x,y:p.y,radius:22,stroke:color(e.payload,'#fff'),lineWidth:2,alpha:.58})}return Object.freeze(out)}}

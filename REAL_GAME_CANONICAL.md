# NOVA TANKS canonical production identity

The production game is the real pre-NOVASTAR NOVA TANKS lineage materialized through `index.html`, `nova-gz/`, and `nova-updates/`.

Do not promote NOVASTAR/Foundation reconstruction branches into `main`. A canonical shipping page contains `__bootModule` and the versioned `nova-updates/` chain and is not a Vite shell that boots `/src/main.ts` or `%BASE_URL%`.

Emergency recovery reference: `production/REAL-NOVA-TANKS`.
